pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "Hello jenkins"'
                sh 'date'
                sh 'echo "changes made"'
            }
        }
    }
}